## What does this PR do?

<!-- One or two sentences. Link related issues with "Closes #123" if applicable. -->

## How was this tested?

<!-- Manual steps you took, RPC methods exercised against a real Firo node. Note anything you couldn't test. -->

## Checklist

- [ ] `pnpm lint` passes
- [ ] `pnpm typecheck` passes
- [ ] `pnpm test` passes (requires `.env.test` with a reachable Firo RPC node)
- [ ] `pnpm build` passes
- [ ] No unrelated changes bundled in (formatting-only diffs, unrelated refactors, etc.)
